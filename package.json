{
  "name": "vortex-protocol",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "turbo run preview",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "db:push": "turbo run db:push",
    "db:generate": "turbo run generate",
    "db:studio": "turbo run db:studio",
    "clean": "rimraf packages/*/dist packages/*/.turbo packages/*/node_modules .turbo"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "rimraf": "^6.0.1",
    "turbo": "^2.1.3",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.3.4"
  },
  "packageManager": "bun@1.3.4"
}

