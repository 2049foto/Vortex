{
  "framework": "vite",
  "buildCommand": "bun run build",
  "outputDirectory": "dist",
  "installCommand": "bun install",
  "devCommand": "bun run dev",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://vortex-backend.vercel.app/api/:path*"
    }
  ]
}

